<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>All Notes</title>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
<style>
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: Arial, sans-serif;
        background: #34495e;
        min-height: 100vh;
        padding: 20px;
    }

    .container {
        background: #ecf0f1;
        padding: 25px;
        border-radius: 8px;
        max-width: 1000px;
        margin: 0 auto;
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
    }

    h1 {
        color: #2c3e50;
        text-align: center;
        margin-bottom: 20px;
        font-size: 28px;
    }

    .add-note {
        background: #27ae60;
        color: white;
        padding: 10px 20px;
        text-decoration: none;
        border-radius: 4px;
        display: inline-block;
        margin-bottom: 20px;
    }

    .add-note:hover {
        background: #229954;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        background: white;
        border-radius: 4px;
        overflow: hidden;
    }

    th {
        background: #3498db;
        color: white;
        padding: 12px;
        text-align: left;
    }

    td {
        padding: 12px;
        border-bottom: 1px solid #bdc3c7;
    }

    tr:nth-child(even) {
        background: #f8f9fa;
    }

    .edit {
        background: #f39c12;
        color: white;
        padding: 6px 12px;
        text-decoration: none;
        border-radius: 3px;
        margin-right: 5px;
    }

    .edit:hover {
        background: #e67e22;
    }

    .delete {
        background: #e74c3c;
        color: white;
        padding: 6px 12px;
        text-decoration: none;
        border-radius: 3px;
    }

    .delete:hover {
        background: #c0392b;
    }
</style>
</head>
<body>
<div class="container">
    <h1>All Notes</h1>
    <a href="/add-note" class="add-note">Add Note</a>
    <table>
        <thead>
            <tr>
                <th>No</th>
                <th>Title</th>
                <th>Content</th>
                <th>TimeStamp</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% for(var i=0; i<mydata.length; i++) { %>
                <tr>
                    <td data-label="No"><%= i+1 %></td>
                    <td data-label="Title"><%= mydata[i].title %></td>
                    <td data-label="Content"><%= mydata[i].content %></td>
                    <td data-label="TimeStamp"><%= mydata[i].timestamp.toDateString() %></td>
                    <td data-label="Actions">
                        <a href="/edit-note/<%= mydata[i]._id %>" class="edit">Edit</a>
                        <a href="/delete-note/<%= mydata[i]._id %>" class="delete">Delete</a>
                    </td>
                </tr>
            <% } %>
        </tbody>
    </table>
</div>
</body>
</html>
